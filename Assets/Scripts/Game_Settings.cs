
public static class Game_Settings
{
    public static float BASE_SPACESHIP_SPEED = 0.6f;
    // fast tempo game -> 1.5f
    // slow, tactical game -> 0.6f
    

    public static float BASE_PLANET_GROWTH_RATE = 3f;
    // fast tempo game -> 10f
    // slow, tactical game -> 3f
    
    
    public static float DEFAULT_TURRET_RADIUS = 2f;

    // planet pixel size ratio to planet_size variable
}